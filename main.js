/**
 * Function for converting decimal numbers to hexidecimal.
 * @param {string} number
 *
 **/

deciToHexi = (number) => {
    return number.toString(16).toUpperCase();
}
console.log(deciToHexi(123));

